I thought building an AI news aggregator where I can take multiple solutions where, for example, uh, YouTube channels and I want, for example, blog posts from open AI and and topic, and I want to scrape those, put them into a database where we have some kind of structure, where we have sources we have let's call them articles. And then what I want to do is I want to run a daily digest where we're going to take all of the articles from within that campaign, and we're going to go another summary around that, and then based on the user task, it specifies some kind of agency background. We can generate a daily digest, which is going to be short snippets to the link to the original source. Now from the YouTube channels, I want to to be able to create these channels, and then we want to get the latest videos from those channels. I think we can use the YouTube RSS feed for that and for that blog post. We can have the URLs that you can scrape for that. Okay? So I want everything here built in a Python backeng. I want to use a Postgres SQL database, I want to use SQL Alchemy in order to define the database models and then also to get the tables. I want the project structure to be an app for the... all the app project is in, and then I also want a Docker folder where we just create a very minimal setup for the postgres SQL database setup. And then that would probably be the starting point. We want to make sure that data down the line that is in the app to render and then also schedule with every twenty four hours to run the reports and everything. And then when we create the data digest, I want to send an email to my personal inbox with this. So that's what I have in mind. I I thought that would be good.